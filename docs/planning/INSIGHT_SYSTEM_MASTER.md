# 인사이트 시스템 마스터 설계서

> 버전: 1.0 | 작성일: 2024-12-25
> "활동하면 자연스럽게 나를 알게 되는" 경험 설계

---

## 1. 핵심 컨셉

### 1.1 차별화 포인트

```
┌─────────────────────────────────────────────────────────────┐
│                    일반 AI 채팅                              │
│  "나에 대해 분석해줘" → 한 번 대화로 끝                        │
│  • 사용자가 "말한 것"만 분석                                  │
│  • 자기 인식 편향 그대로 반영                                 │
└─────────────────────────────────────────────────────────────┘
                              vs
┌─────────────────────────────────────────────────────────────┐
│                    우리 앱                                   │
│  테스트 + 퀴즈 + 투표 참여 → 3개월간 데이터 축적               │
│  • 사용자가 "행동한 것" 분석                                  │
│  • 무의식적 선택 패턴 발견                                    │
│  • 시간에 따른 변화 추적                                      │
│  → AI 채팅보다 더 정확한 프로필                               │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 사용자 여정

```
Day 1: 테스트 1개 완료
       → "기본 성향" 인사이트 즉시 해금
       → "3개 더 하면 '성격 조합' 해금!" 유도

Week 1: 테스트 3개 + 투표 5개
       → "성격 조합" 해금
       → "판단 스타일" 해금
       → "10개 더 하면 '숨은 패턴' 해금!"

Week 2-4: 테스트 5개 + 퀴즈 10개 + 투표 15개
       → "관계 패턴" 해금
       → "관심사 지도" 해금

Month 2+: 30개 이상 활동
       → "숨은 패턴" 해금
       → "성장 여정" 시작
       → "AI 종합 분석" 가능 (유료)
```

---

## 2. 인사이트 해금 단계 (7단계)

### 2.1 전체 구조

| 단계 | 인사이트 | 해금 조건 | 분석 방식 | 비용 |
|-----|---------|----------|----------|-----|
| 1 | 기본 성향 | 테스트 1개 | 집계 | 무료 |
| 2 | 성격 조합 | 테스트 3개 | 룰 매칭 | 무료 |
| 3 | 판단 스타일 | 투표 10개 | 태그 집계 | 무료 |
| 4 | 관심사 지도 | 활동 15개 | 집계 | 무료 |
| 5 | 관계 패턴 | 관계 활동 10개 | 룰 매칭 | 무료 |
| 6 | 숨은 패턴 | 활동 30개 | 룰 매칭 | 무료 |
| 7 | AI 종합 분석 | Stage 6 해금 + 결제 | AI 생성 | 유료 |

> **Stage 7 설계 의도**: 30개 활동으로 충분한 데이터가 축적되어야 AI 분석 품질이 보장됨.
> 무료 인사이트(1-6)로 가치를 경험한 후 자연스러운 유료 전환 유도.

### 2.2 각 단계 상세

---

#### Stage 1: 기본 성향 (즉시)
**해금**: 테스트 1개 완료

**표시 내용**:
```
┌─────────────────────────────────────┐
│ 📊 기본 성향                         │
├─────────────────────────────────────┤
│                                     │
│  🐱 고양이 케미: 철학 냥이            │
│  "깊이 생각하고 조용히 관찰하는 타입"  │
│                                     │
│  📈 차원별 점수                      │
│  호기심 ████████░░ 80%              │
│  경계심 ██████░░░░ 60%              │
│  보스기질 ████░░░░░░ 40%            │
│                                     │
└─────────────────────────────────────┘
```

**유도 메시지**:
> "테스트 2개 더 하면 **성격 조합** 인사이트가 열려요!"

**데이터 소스**: `resultService.getMyResults()`
**구현 난이도**: ⭐ (이미 가능)

---

#### Stage 2: 성격 조합 (테스트 3개)
**해금**: 서로 다른 테스트 3개 완료

**표시 내용**:
```
┌─────────────────────────────────────┐
│ 🔮 성격 조합                         │
├─────────────────────────────────────┤
│                                     │
│  💫 조용한 관찰자                    │
│                                     │
│  "혼자만의 시간을 소중히 여기고,      │
│   깊이 있는 관계를 선호해요.          │
│   고양이처럼 독립적이지만,            │
│   신뢰하는 사람에겐 따뜻해요."        │
│                                     │
│  기반: 고양이(독립형) + 갈등(회피형)   │
│       + 커피(혼자형)                 │
│                                     │
│  🔬 심리학 근거                      │
│  Texas 대학 연구: 고양이 선호자는     │
│  개방성 +11%, 독립성 높음            │
│                                     │
└─────────────────────────────────────┘
```

**유도 메시지**:
> "투표 10개 참여하면 **판단 스타일**이 보여요!"

**데이터 소스**: 테스트 결과 조합 + 룰 매칭
**구현**: 60개 룰 필요

---

#### Stage 3: 판단 스타일 (투표 10개)
**해금**: 태그된 투표 10개 이상 참여

**표시 내용**:
```
┌─────────────────────────────────────┐
│ ⚖️ 판단 스타일                       │
├─────────────────────────────────────┤
│                                     │
│  실용 ████████░░ 70%  vs 감성 30%   │
│  안전 ████░░░░░░ 40%  vs 모험 60%   │
│  혼자 ██████░░░░ 55%  vs 함께 45%   │
│                                     │
│  💡 해석                            │
│  "결정할 때 실용을 중시하지만,        │
│   새로운 도전은 두려워하지 않아요.    │
│   혼자 결정하는 걸 편하게 느끼지만    │
│   중요한 건 함께 상의해요."          │
│                                     │
└─────────────────────────────────────┘
```

**유도 메시지**:
> "활동 5개 더 하면 **관심사 지도**가 완성돼요!"

**데이터 소스**: 투표 선택 태그 집계
**구현**: 투표에 태그 추가 필요

---

#### Stage 4: 관심사 지도 (활동 15개)
**해금**: 테스트 + 퀴즈 + 투표 합계 15개

**표시 내용**:
```
┌─────────────────────────────────────┐
│ 🗺️ 관심사 지도                       │
├─────────────────────────────────────┤
│                                     │
│  🐱 고양이      ████████████ 35%    │
│  💑 관계/심리   ████████░░░░ 25%    │
│  ☕ 라이프스타일 ██████░░░░░░ 18%    │
│  🐕 강아지      ████░░░░░░░░ 12%    │
│  🌿 식물        ██░░░░░░░░░░ 10%    │
│                                     │
│  💡 인사이트                         │
│  "고양이와 관계 심리에 관심이 많아요. │
│   아마 독립적이면서도 깊은 관계를     │
│   중요하게 생각하는 분 같아요."       │
│                                     │
└─────────────────────────────────────┘
```

**데이터 소스**: 카테고리별 참여 집계
**구현 난이도**: ⭐ (이미 가능)

---

#### Stage 5: 관계 패턴 (관계 활동 10개)
**해금**: conflictStyle 테스트 OR 관계 활동 10개

> **관계 활동 정의**:
> - 관계 테스트: `conflictStyle`, `idealType`
> - 관계 태그 콘텐츠: RELATIONSHIP_TAGS 중 하나라도 포함된 투표/퀴즈 참여
> - 태그 정의는 `src/app/dashboard/data/insight-system.ts`의 `RELATIONSHIP_TAGS` 참조

**표시 내용**:
```
┌─────────────────────────────────────┐
│ 💬 관계 패턴                         │
├─────────────────────────────────────┤
│                                     │
│  🎭 표현 스타일: 신중한 배려형        │
│                                     │
│  • 갈등 시: 회피 → 시간 후 대화       │
│  • 감정 표현: 행동으로 보여주는 편    │
│  • 상대 배려: 내 감정보다 상대 먼저   │
│                                     │
│  📊 투표 패턴에서 발견               │
│  "직접 말하기" 선택: 30%             │
│  "돌려 말하기" 선택: 70%             │
│                                     │
│  💡 인사이트                         │
│  "마음을 직접 표현하기보다            │
│   행동으로 보여주는 걸 선호해요.      │
│   갈등보다는 조화를 중시하지만,       │
│   중요한 건 꼭 말하려고 노력해요."    │
│                                     │
└─────────────────────────────────────┘
```

**데이터 소스**: situation-reaction 태그 + 룰 매칭
**구현**: 관계 관련 태그 분석

---

#### Stage 6: 숨은 패턴 (활동 30개)
**해금**: 총 활동 30개 이상
**분석 방식**: 룰 매칭 (교차 분석 + 시간대 패턴)

**표시 내용**:
```
┌─────────────────────────────────────┐
│ 🔍 숨은 패턴                         │
├─────────────────────────────────────┤
│                                     │
│  🎭 발견된 모순                      │
│                                     │
│  "고양이 테스트에선 독립성을 중시하는데│
│   이상형 테스트에선 밀착형을 원해요.  │
│                                     │
│   → 평소엔 혼자 시간이 필요하지만,    │
│     연인에겐 깊은 친밀감을 원하는     │
│     '선택적 친밀' 타입이에요."       │
│                                     │
│  ⏰ 시간대 패턴                      │
│  "밤 10시 이후 투표에서              │
│   '감성적' 선택이 40% 증가해요.      │
│   피곤할 때 감정에 솔직해지시네요."   │
│                                     │
└─────────────────────────────────────┘
```

**데이터 소스**: 교차 분석 + 시간대 분석
**구현**: AI 활용 권장 (복잡한 패턴)

---

#### Stage 7: AI 종합 분석 (유료)
**해금**: Stage 6 해금 완료 + 결제
**전제 조건**: 활동 30개 이상 (Stage 6 조건 충족)

**표시 내용**:
```
┌─────────────────────────────────────┐
│ 🤖 AI 종합 분석 리포트               │
│    (by Claude)                      │
├─────────────────────────────────────┤
│                                     │
│  📋 3개월간의 데이터 기반 분석        │
│                                     │
│  ═══════════════════════════════════│
│  1. 핵심 성격 프로필                 │
│  ─────────────────────────────────  │
│  당신은 "사려 깊은 독립주의자"입니다. │
│                                     │
│  혼자만의 시간을 통해 에너지를 충전   │
│  하지만, 신뢰하는 소수와의 깊은 관계  │
│  를 매우 소중히 여깁니다...          │
│                                     │
│  ═══════════════════════════════════│
│  2. 강점과 성장 포인트                │
│  ─────────────────────────────────  │
│  ✨ 강점: 공감력, 분석력, 일관성      │
│  🌱 성장: 직접적 표현, 유연성         │
│                                     │
│  ═══════════════════════════════════│
│  3. 관계에서의 특징                  │
│  ...                                │
│                                     │
│  ═══════════════════════════════════│
│  4. 추천 행동                        │
│  ...                                │
│                                     │
└─────────────────────────────────────┘
```

**비용**: Claude Haiku 기준 ~$0.01-0.02/회
**구현**: AI 프롬프트 + 템플릿

---

## 3. 태그 시스템 설계

### 3.1 태그 정의 (Single Source of Truth)

> **중요**: 태그 정의의 단일 소스는 코드입니다.
> 상세 정의: `src/app/dashboard/data/insight-system.ts`

| 태그 유형 | 용도 | 기반 이론 |
|----------|------|----------|
| PERSONALITY_TAGS | 테스트 결과 분류 | Big Five (OCEAN) |
| DECISION_TAGS | 투표 선택지 분류 | 의사결정 스타일 |
| RELATIONSHIP_TAGS | 관계 패턴 분석 | TKI 갈등 모델 |

**코드 참조**:
```typescript
// src/app/dashboard/data/insight-system.ts 에서 발췌
export const PERSONALITY_TAGS = [
  'extroverted', 'introverted', 'ambiverted',  // 에너지 방향
  'logical', 'emotional', 'intuitive', 'analytical',  // 정보 처리
  'planned', 'spontaneous', 'flexible', 'structured',  // 행동 방식
  'independent', 'collaborative', 'supportive', 'leading',  // 관계 스타일
  'resilient', 'sensitive',  // 정서 안정성 (Big Five 신경성)
] as const;

export const DECISION_TAGS = [
  'practical', 'sentimental',  // 실용 vs 감성 (emotional과 구분)
  'safe', 'adventurous',       // 안전 vs 모험
  'solo', 'together',          // 혼자 vs 함께
  'direct', 'indirect',        // 직접 vs 우회
  'present-focused', 'future-focused',  // 현재 vs 미래
] as const;

export const RELATIONSHIP_TAGS = [
  'expressive', 'reserved',  // 표현 스타일
  'competing', 'avoiding', 'accommodating', 'collaborating', 'compromising',  // TKI 5유형
  'close-bonding', 'space-needing',  // 친밀도 선호
  'self-first', 'other-first',  // 감정 처리
] as const;
```

### 3.2 태그 적용 예시

**투표 선택지 태그**:
```typescript
// 야식 먹을까 말까?
optionA: { tags: { decision: ['sentimental', 'present-focused'], personality: ['spontaneous'] } }
optionB: { tags: { decision: ['practical', 'future-focused'], personality: ['planned'] } }
```

**테스트 결과 태그** (Phase 1에서 전체 매핑 예정):
```typescript
// 고양이 테스트 결과 예시
"철학냥이": { personality: ['introverted', 'analytical', 'independent'], strength: ['깊은 사고'], growth: ['표현력'] }
"애교냥이": { personality: ['extroverted', 'emotional', 'collaborative'], strength: ['친화력'], growth: ['독립성'] }
```

### 3.3 태그 우선 적용 대상

| 우선순위 | 대상 | 수량 | 작업량 |
|---------|------|------|--------|
| 1 | 투표 (VS Poll) | ~50개 | 옵션당 2-3태그 |
| 2 | 상황반응 (Situation) | ~100개 | 이미 태그 있음 (확장) |
| 3 | 주요 테스트 결과 | 10개×10결과 | 결과당 3-5태그 |
| 4 | 퀴즈 | ~30개 | 카테고리 태그만 |

---

## 4. 룰 엔진 설계

### 4.1 룰 구조

```typescript
interface InsightRule {
  id: string;
  name: string;
  priority: number;  // 낮을수록 우선

  // 조건
  conditions: {
    // 테스트 결과 조건
    tests?: {
      [testKey: string]: {
        results?: string[];           // 결과 이름
        dimensions?: {
          [dim: string]: ('high' | 'medium' | 'low')[];
        };
        tags?: string[];              // 결과 태그
      };
    };

    // 투표/퀴즈 태그 조건
    decisionPattern?: {
      tag: string;
      minRatio: number;  // 0-1
    }[];

    // 관심사 조건
    interests?: {
      category: string;
      minCount: number;
    }[];

    // 최소 데이터 요건
    minActivities?: number;
  };

  // 결과
  insight: {
    title: string;
    emoji: string;
    description: string;
    explanation?: string;  // 왜 이런 결론인지
    actionTip?: string;    // 실천 팁
  };

  // 메타
  confidence: 'high' | 'medium' | 'low';
  source?: string;  // 심리학 연구 출처
  category: 'personality' | 'lifestyle' | 'relationship' | 'hidden';
}
```

### 4.2 룰 카테고리별 수량 계획

| 카테고리 | 룰 수 | 예시 |
|---------|-------|------|
| 성격 기본 | 15개 | 외향/내향 조합, 에너지 패턴 |
| 성격 조합 | 15개 | 2-3개 테스트 교차 분석 |
| 라이프스타일 | 10개 | 취향 패턴, 루틴 성향 |
| 관계/소통 | 10개 | 갈등 대처, 표현 스타일 |
| 숨은 패턴 | 10개 | 모순 발견, 시간대 패턴 |
| **총합** | **60개** | |

### 4.3 예시 룰 (10개)

```typescript
const SAMPLE_RULES: InsightRule[] = [
  // ═══════════════════════════════════════
  // 성격 기본 (5개)
  // ═══════════════════════════════════════
  {
    id: 'personality-001',
    name: '조용한 관찰자',
    priority: 10,
    conditions: {
      tests: {
        cat: { tags: ['introverted', 'analytical'] },
        human: { dimensions: { inssa: ['low', 'medium'] } }
      }
    },
    insight: {
      title: '조용한 관찰자',
      emoji: '🌙',
      description: '혼자만의 시간을 소중히 여기고, 깊이 있는 관계를 선호해요.',
      explanation: '고양이 성향 + 낮은 인싸력 조합',
      actionTip: '소규모 모임이나 1:1 대화가 더 편할 수 있어요'
    },
    confidence: 'high',
    source: 'Texas University Pet Study',
    category: 'personality'
  },

  {
    id: 'personality-002',
    name: '에너지 넘치는 사교가',
    priority: 10,
    conditions: {
      tests: {
        dog: { tags: ['extroverted', 'collaborative'] },
        human: { dimensions: { inssa: ['high'] } }
      }
    },
    insight: {
      title: '에너지 넘치는 사교가',
      emoji: '☀️',
      description: '사람들과 어울리며 에너지를 얻어요. 새로운 만남을 즐기죠.',
      actionTip: '다양한 모임에 참여해보세요, 그게 충전이에요'
    },
    confidence: 'high',
    category: 'personality'
  },

  {
    id: 'personality-003',
    name: '계획적 실행가',
    priority: 15,
    conditions: {
      tests: {
        human: { dimensions: { plan: ['high'] } }
      },
      decisionPattern: [
        { tag: 'practical', minRatio: 0.6 }
      ]
    },
    insight: {
      title: '계획적 실행가',
      emoji: '📋',
      description: '목표를 세우고 차근차근 실행해요. 즉흥보다는 준비를 선호하죠.',
      actionTip: '가끔은 계획 없이 흘러가보는 것도 좋아요'
    },
    confidence: 'medium',
    category: 'personality'
  },

  {
    id: 'personality-004',
    name: '유연한 탐험가',
    priority: 15,
    conditions: {
      tests: {
        human: { dimensions: { adventure: ['high'] } }
      },
      decisionPattern: [
        { tag: 'adventurous', minRatio: 0.5 }
      ]
    },
    insight: {
      title: '유연한 탐험가',
      emoji: '🗺️',
      description: '새로운 경험을 즐기고 변화에 유연해요. 루틴보다 다양성을 추구하죠.',
      actionTip: '새로운 취미나 여행을 계획해보세요'
    },
    confidence: 'medium',
    category: 'personality'
  },

  {
    id: 'personality-005',
    name: '공감의 달인',
    priority: 15,
    conditions: {
      tests: {
        human: { dimensions: { empathy: ['high'] } },
        conflictStyle: { tags: ['collaborative', 'other-first'] }
      }
    },
    insight: {
      title: '공감의 달인',
      emoji: '💗',
      description: '상대방의 감정을 잘 읽고 공감해요. 관계에서 중재자 역할을 하기도 하죠.',
      actionTip: '자신의 감정도 돌보는 시간을 가져보세요'
    },
    confidence: 'high',
    category: 'personality'
  },

  // ═══════════════════════════════════════
  // 라이프스타일 (3개)
  // ═══════════════════════════════════════
  {
    id: 'lifestyle-001',
    name: '조용한 돌봄이',
    priority: 20,
    conditions: {
      interests: [
        { category: 'fish', minCount: 3 },
        { category: 'plant', minCount: 3 }
      ]
    },
    insight: {
      title: '고요한 생명 지킴이',
      emoji: '🌿',
      description: '조용히 생명을 돌보는 것에서 평화를 느껴요. 관상어와 식물처럼 말없이 함께하는 존재를 좋아해요.',
      actionTip: '작은 테라리움이나 어항이 힐링이 될 거예요'
    },
    confidence: 'medium',
    category: 'lifestyle'
  },

  {
    id: 'lifestyle-002',
    name: '루틴 러버',
    priority: 20,
    conditions: {
      tests: {
        coffee: { results: ['아메리카노', '모닝커피형'] }
      },
      decisionPattern: [
        { tag: 'practical', minRatio: 0.6 },
        { tag: 'structured', minRatio: 0.5 }
      ]
    },
    insight: {
      title: '안정의 루티너',
      emoji: '☕',
      description: '매일 같은 커피, 익숙한 루틴에서 편안함을 느껴요. 예측 가능한 일상이 힘이 되죠.',
      actionTip: '루틴 안에서 작은 변화를 시도해보세요'
    },
    confidence: 'medium',
    category: 'lifestyle'
  },

  // ═══════════════════════════════════════
  // 숨은 패턴 (2개)
  // ═══════════════════════════════════════
  {
    id: 'hidden-001',
    name: '선택적 친밀',
    priority: 5,
    conditions: {
      tests: {
        cat: { tags: ['independent'] },
        idealType: { tags: ['close-bonding'] }
      }
    },
    insight: {
      title: '독립과 친밀 사이',
      emoji: '🎭',
      description: '평소엔 혼자 시간이 필요하지만, 연인에게는 깊은 친밀감을 원해요. 가까운 사람에겐 다른 모습을 보여주는 타입.',
      explanation: '고양이 성향(독립)과 이상형(밀착)의 대비가 흥미로운 내면을 보여줘요'
    },
    confidence: 'high',
    category: 'hidden'
  },

  {
    id: 'hidden-002',
    name: '계획된 모험가',
    priority: 5,
    conditions: {
      tests: {
        human: { dimensions: { plan: ['high'] } }
      },
      decisionPattern: [
        { tag: 'adventurous', minRatio: 0.5 }
      ]
    },
    insight: {
      title: '안전한 모험가',
      emoji: '🧭',
      description: '겉으로는 신중해 보이지만, 안전한 범위 안에서 새로운 걸 시도해요. 계획된 탈선을 즐기는 타입.',
      explanation: '계획형 성격이지만 투표에서 모험적 선택이 많음'
    },
    confidence: 'medium',
    category: 'hidden'
  }
];
```

---

## 5. AI 분석 설계

### 5.1 비용 계산

| 모델 | 입력 토큰 | 출력 토큰 | 예상 비용/회 |
|-----|----------|----------|------------|
| Claude Haiku | ~2,000 | ~500 | $0.01 |
| Claude Sonnet | ~2,000 | ~500 | $0.02 |
| GPT-4o-mini | ~2,000 | ~500 | $0.01 |

**월간 비용 추정**:
- MAU 10,000 × 10% AI 사용 = 1,000회
- 1,000 × $0.01 = **$10/월**

### 5.2 AI 프롬프트

```typescript
const AI_INSIGHT_PROMPT = `
당신은 친근하고 따뜻한 성격 분석가입니다.
아래 사용자의 활동 데이터를 보고 깊이 있는 인사이트를 제공해주세요.

## 사용자 데이터

### 테스트 결과
{{testResults}}

### 투표 패턴
- 실용 vs 감성: {{practicalRatio}}% vs {{emotionalRatio}}%
- 안전 vs 모험: {{safeRatio}}% vs {{adventurousRatio}}%
- 혼자 vs 함께: {{soloRatio}}% vs {{togetherRatio}}%

### 관심사 (참여 순)
{{topCategories}}

### 관계 상황 반응 패턴
{{relationshipPatterns}}

### 활동 기간
{{activityPeriod}}

## 요청

다음 형식으로 종합 분석을 작성해주세요:

### 1. 핵심 성격 (2-3문장)
이 사람의 핵심 성향을 자연스럽게 설명

### 2. 강점 (3가지)
- 강점1: 설명
- 강점2: 설명
- 강점3: 설명

### 3. 성장 포인트 (2가지)
부정적이지 않게, 발전 가능성으로 표현

### 4. 관계에서의 특징 (2-3문장)
연애, 친구, 가족 관계에서의 패턴

### 5. 흥미로운 발견 (1가지)
데이터에서 발견한 재미있거나 의외인 점

### 6. 추천 행동 (2가지)
구체적이고 실천 가능한 조언

## 톤 가이드
- 자연스럽고 친근한 말투 ("~해요", "~네요")
- 부정적 표현 대신 성장 가능성으로
- 너무 길지 않게, 핵심만 전달
- 데이터 기반 구체적 언급 포함
`;
```

### 5.3 AI 분석 트리거

```typescript
interface AIAnalysisTrigger {
  // 필수 조건
  minTests: 3;
  minActivities: 20;

  // 선택 조건 (하나 이상)
  optional: {
    paidUser: boolean;
    monthlyReport: boolean;  // 월 1회
    manualRequest: boolean;  // 버튼 클릭
  };
}
```

---

## 6. DB 스키마

### 6.1 Turso 테이블

```sql
-- 1. 인사이트 규칙
CREATE TABLE insight_rules (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  priority INTEGER DEFAULT 50,
  conditions JSON NOT NULL,
  insight JSON NOT NULL,
  confidence TEXT CHECK(confidence IN ('high', 'medium', 'low')),
  category TEXT NOT NULL,
  source TEXT,
  active INTEGER DEFAULT 1,
  version INTEGER DEFAULT 1,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 2. 테스트 결과 태그
CREATE TABLE result_tags (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  test_key TEXT NOT NULL,
  result_name TEXT NOT NULL,
  tags JSON NOT NULL,  -- {"personality": [...], "strength": [...]}
  version INTEGER DEFAULT 1,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  UNIQUE(test_key, result_name)
);

-- 3. 투표 태그
CREATE TABLE poll_tags (
  poll_id TEXT PRIMARY KEY,
  option_a_tags JSON,
  option_b_tags JSON,
  version INTEGER DEFAULT 1,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 4. 사용자 인사이트 캐시
CREATE TABLE user_insights (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id TEXT NOT NULL,
  insight_type TEXT NOT NULL,  -- 'basic', 'combination', 'decision', etc.
  insight_data JSON NOT NULL,
  matched_rules JSON,  -- 매칭된 룰 ID 목록
  generated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  expires_at TIMESTAMP,
  UNIQUE(user_id, insight_type)
);

-- 5. AI 분석 결과
CREATE TABLE ai_analyses (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id TEXT NOT NULL,
  input_data JSON NOT NULL,
  output_text TEXT NOT NULL,
  model TEXT NOT NULL,
  tokens_used INTEGER,
  cost_usd REAL,
  generated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 6. 분포 통계 (비교용)
CREATE TABLE demographic_stats (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  test_key TEXT NOT NULL,
  result_name TEXT NOT NULL,
  age_group TEXT,
  gender TEXT,
  count INTEGER DEFAULT 0,
  ratio REAL,  -- 해당 그룹 내 비율
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  UNIQUE(test_key, result_name, age_group, gender)
);

-- 인덱스
CREATE INDEX idx_rules_priority ON insight_rules(priority);
CREATE INDEX idx_rules_category ON insight_rules(category);
CREATE INDEX idx_user_insights_user ON user_insights(user_id);
CREATE INDEX idx_stats_test ON demographic_stats(test_key);
```

### 6.2 localStorage 구조

```typescript
interface LocalInsightStorage {
  // 캐시된 인사이트
  insights: {
    [insightType: string]: {
      data: InsightData;
      matchedRules: string[];
      generatedAt: string;
      expiresAt: string;
    };
  };

  // 해금 상태
  unlockStatus: {
    [insightType: string]: {
      unlocked: boolean;
      unlockedAt?: string;
      progress: number;  // 0-100
      remaining: string;  // "투표 3개 더"
    };
  };

  // 태그 집계 (실시간)
  tagCounts: {
    decision: { [tag: string]: number };
    personality: { [tag: string]: number };
    relationship: { [tag: string]: number };
  };
}
```

---

## 7. 구현 로드맵

### Phase 1: 기반 시스템 (2주)

**Week 1** (태그 시스템 - 필수 선행):
- [ ] **[필수] 테스트 결과 태그 매핑 (주요 10개 테스트)** ← 룰 엔진 작동에 필수
- [ ] 태그 타입 정의 (이미 `insight-system.ts`에 존재, 필요시 분리)
- [ ] InsightRule 인터페이스 구현

**Week 2**:
- [ ] InsightService 기본 구조
- [ ] 해금 조건 체크 로직
- [ ] localStorage 저장/로드

### Phase 2: 인사이트 1-4단계 (2주)

**Week 3**:
- [ ] Stage 1: 기본 성향 (즉시 가능)
- [ ] Stage 2: 성격 조합 (룰 15개)
- [ ] 인사이트 UI 컴포넌트

**Week 4**:
- [ ] 투표 태그 추가 (50개)
- [ ] Stage 3: 판단 스타일
- [ ] Stage 4: 관심사 지도
- [ ] 해금 유도 UI

### Phase 3: 인사이트 5-6단계 (2주)

**Week 5**:
- [ ] 관계 태그 확장
- [ ] Stage 5: 관계 패턴 (룰 10개)
- [ ] Turso DB 마이그레이션

**Week 6**:
- [ ] Stage 6: 숨은 패턴 (룰 10개)
- [ ] 시간대 분석 로직
- [ ] 교차 분석 로직

### Phase 4: AI 분석 (1주)

**Week 7**:
- [ ] AI 프롬프트 최적화
- [ ] Stage 7: AI 종합 분석
- [ ] 비용 모니터링
- [ ] 유료화 결제 연동 (선택)

---

## 8. 성공 지표

### 8.1 사용자 행동

| 지표 | 목표 | 측정 방법 |
|-----|------|----------|
| 인사이트 페이지 진입률 | 30%+ | 메인 대비 |
| 해금 유도 클릭률 | 20%+ | 유도 메시지 클릭 |
| 30개 활동 달성률 | 15%+ | 숨은 패턴 해금 |
| AI 분석 요청률 | 5%+ | 유료 전환 |

### 8.2 품질

| 지표 | 목표 | 측정 방법 |
|-----|------|----------|
| 인사이트 정확도 | 80%+ | "맞아요" 피드백 |
| 공유율 | 10%+ | SNS 공유 |
| 재방문 (인사이트 목적) | 20%+ | 세션 분석 |

---

## 9. 요약

### 핵심 설계 원칙

1. **점진적 해금**: 활동할수록 더 많은 인사이트
2. **유도 명확화**: "N개 더 하면 해금" 항상 표시
3. **과학적 기반**: 심리학 연구 출처 명시
4. **하이브리드**: 룰 기반(무료) + AI(유료)
5. **데이터 축적**: 시간이 지날수록 정확도 향상

### 필요 리소스

| 항목 | 수량 | 비고 |
|-----|------|------|
| 인사이트 룰 | 60개 | 카테고리별 10-15개 |
| 테스트 결과 태그 | ~100개 | 주요 10개 테스트 |
| 투표 태그 | ~50개 | 기존 투표에 추가 |
| DB 테이블 | 6개 | Turso |
| AI 비용 | ~$10-50/월 | MAU 10,000 기준 |
